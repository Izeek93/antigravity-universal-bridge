# ==============================================================================
# 🛰 Antigravity Universal Bridge — Python Dependencies
# ==============================================================================

# 🔒 Межпроцессное файловое блокирование (IPC Queue Lock)
portalocker>=2.8.2

# ⚡ Доступ к системным API Windows и Single-Instance Named Mutex
pywin32>=306

# ⚙️ Конфигурация и переменные окружения
python-dotenv>=1.0.0

# 📊 Системный мониторинг процессов и ресурсов (RAM/CPU/PID)
psutil>=5.9.0

# 🌐 Сетевые HTTP-запросы
requests>=2.31.0

# 📸 Захват рабочего стола Windows
Pillow>=10.0.0

# --- ОПЦИОНАЛЬНЫЕ ЗАВИСИМОСТИ (для распознавания голосовых сообщений) ---
# faster-whisper>=1.0.0
# soundfile>=0.12.1
